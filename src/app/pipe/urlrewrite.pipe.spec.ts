import { UrlrewritePipe } from './urlrewrite.pipe';

describe('UrlrewritePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlrewritePipe();
    expect(pipe).toBeTruthy();
  });
});
